{
  "name": "kanban-task-manager-fullstack",
  "version": "1.0.0",
  "description": "Full-stack Kanban Task Manager with Angular and Express",
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install",
    "install:all": "npm run install:frontend && npm run install:backend",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "start": "npx serve frontend/dist/my-kanban-project/browser -s -l 4201 -n"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "keywords": ["kanban", "angular", "express", "fullstack"],
  "author": "Harshal Mali",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
